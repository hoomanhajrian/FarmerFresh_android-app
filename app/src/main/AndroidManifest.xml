<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.super7.farmerfresh">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.FarmerFresh">
        <activity android:name=".ui.cart.ActivityCartDetail"></activity>
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GoogleSignInActivity" />
        <activity
            android:name=".OauthActivity"
            android:label="@string/app_name" />
        <activity android:name=".ui.product.ActivityProductDetail" />
        <activity android:name=".ui.filter.ActivityFilter" />
        <activity android:name=".ui.product.ActivityProductList" />
        <activity
            android:name=".AuthActivity"
            android:label="@string/title_activity_auth"
            android:noHistory="true"
            android:theme="@style/Theme.FarmerFresh.NoActionBar" />
        <activity
            android:name=".OnboardingActivity"
            android:noHistory="true" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.orderDetails.ActivityOrderDetails"
            android:label="Order Details" />
    </application>

</manifest>